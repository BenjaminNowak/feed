from setuptools import find_packages, setup

setup(
    name="feed-aggregator",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    description="A simple feed aggregator using Feedly API",
)
